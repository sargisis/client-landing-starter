{
  "name": "client-landing-starter",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix test",
    "build": "npm run build --prefix test",
    "preview": "wrangler dev",
    "lint": "npm run lint --prefix test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
